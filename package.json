{
  "name": "united-trust-mortgage-site",
  "private": true,
  "description": "UnitedTrustMortgage marketing site + quote builder. Netlify auto-installs these deps so esbuild can bundle them into the Functions in netlify/functions/.",
  "type": "module",
  "scripts": {
    "dev": "netlify dev"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}
